import Link from 'next/link';
import {notFound} from 'next/navigation';
import {ArrowUpRight,ArrowLeft,Check} from 'lucide-react';
import {products,divisionName} from '@/lib/products';
import ProductVariants from '@/components/product-variants';
import {Table,TableBody,TableCell,TableRow} from '@/components/ui/table';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(p=>p.slug===slug);return {title:p?.name||'Product not found',description:p?.summary}}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(p=>p.slug===slug);if(!p)notFound();return <main id="main" className="wrap inner-page"><Link className="breadcrumb" href={`/products?division=${p.division}`}><ArrowLeft size={16}/> {divisionName(p.division)}</Link><section className="product-detail"><div className="detail-image"><img src={`/images/${p.image}`} alt={p.name}/><span>{divisionName(p.division)}</span></div><div className="detail-copy"><p className="eyebrow">{p.division==='food'?'EVERYDAY PRODUCTS. THOUGHTFULLY SOURCED.':'THE BUILDING BLOCKS OF YOUR BUSINESS.'}</p><h1>{p.name}</h1><p className="intro">{p.summary}</p><Link href={`/contact?product=${encodeURIComponent(p.name)}`} className="button">Request a quote <ArrowUpRight size={18}/></Link><p className="small-note">Our team can help with specifications, quantities and availability.</p><div className="applications"><h2>Suited to your business</h2>{p.applications.map(a=><p key={a}><Check size={16}/>{a}</p>)}</div></div></section><section className="specifications"><div><p className="eyebrow">KNOW YOUR PRODUCT</p><h2 className="display-heading">The details<br/>that matter.</h2><p>Have a specific requirement? Let’s discuss the right fit before you order.</p></div><Table><TableBody>{p.details.map(([label,value])=><TableRow key={label}><TableCell className="spec-label">{label}</TableCell><TableCell>{value}</TableCell></TableRow>)}</TableBody></Table></section>{p.variants.length>0&&<ProductVariants name={p.name} variants={p.variants} food={p.division==='food'}/>}<section className="catalogue-help"><h2>Let’s talk about your supply.</h2><p>Tell us the product, quantity and delivery location you have in mind.</p><Link className="text-link" href={`/contact?product=${encodeURIComponent(p.name)}`}>Start your enquiry <ArrowUpRight size={18}/></Link></section></main>}
