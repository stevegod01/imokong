# Banner 2 factory production floor sketch

The user selected image 02, Factory production floor, from the original-site image collection. Its preserved original is industrial-images-extraction/IMOKO-industrial-images/industrial-photos/factory-floor.jpg (6720 x 4480), originally served at https://imokong.com/warehouse2.jpg. A faithful 1440 x 960 inspection copy was used as the image-generation reference because the full-size source exceeded the image viewer's transfer limit. The original photograph is unchanged.

The built-in Imagegen tool converted that reference into a monochrome architectural sketch preserving the factory's roof trusses, windows, mezzanine, machinery, conveyors and elevated perspective. The exact prompt is saved as design/factory-floor-sketch-prompt.txt. The selected project asset is public/images/banner-factory-floor-sketch.png.

The sketch is rendered only on the supply slide (banner 2), as an absolute decorative underlay above the grid and behind the text, links and animated warehouse/forklift scene. It uses the same soft blending and edge fades as banner 1: 22% opacity on desktop and 15% on mobile, with an additional horizontal fade behind the copy. Multiply blending lets white negative space merge with the banner background; native transparency is preserved if present in the generated file.

The layer does not affect layout, intercept clicks or appear in the accessibility tree. The warehouse lettering, off-white logos, Palm Bright stock, forklift sequence, banner size and six-second carousel timing remain unchanged.
